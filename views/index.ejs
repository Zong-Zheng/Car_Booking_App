<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Car Booking</title>
</head>
<body>
    <%- include ('bar/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Bookings</h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Booking ID</th>
                    <th>Number</th>
                    <th>Booking Date</th>
                    <th>Booked By</th>
                    <th>Start Date</th>
                    <th>End Date</th>
                    <th>Style</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                <!-- TO DO: Insert code here to display the students information by looping through the students array-->
                <%for (let i = 0; i<bookings.length; i++) {%>
                    <tr>
                        <td><%= bookings[i].booking_id %></td>
                        <td><a href="/bookings/<%=bookings[i].booking_id%>"><%=bookings[i].number %></a></td>
                        <td><%= bookings[i].bookingdate.toISOString().split('T')[0] %></td>
                        <td><%= bookings[i].bookedby %></td>
                        <td><%= bookings[i].startdate.toISOString().split('T')[0] %></td>
                        <td><%= bookings[i].enddate.toISOString().split('T')[0] %></td>
                        <td><img src="images/<%=bookings[i].image%>" width="20%" alt="AAA"></td>
                        <td><a href="/bookings/<%=bookings[i].booking_id%>/update">Edit</a></td>
                        <td><a href="/bookings/<%=bookings[i].booking_id%>/delete" onclick="return confirm('Are you sure you want to delete this ?')">Delete</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>